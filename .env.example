# ==========================================
# إعدادات نظام حجز الفحص الفني
# انسخ هذا الملف إلى .env وعدّل القيم
# ==========================================

# المنفذ (Render يضبطه تلقائياً)
PORT=3000

# كلمة مرور لوحة التحكم (غيّرها!)
ADMIN_PASSWORD=Admin@2024

# مفتاح تشفير JWT (غيّره لسلسلة عشوائية طويلة!)
JWT_SECRET=change-this-to-a-random-secret-key-2024

# مسار قاعدة البيانات (اتركه كما هو)
DB_PATH=./database.db
